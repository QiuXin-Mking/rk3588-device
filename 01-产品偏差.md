# 产品需求偏差与待确认事项

本文汇总当前屏幕端产品需求与现有实现之间的偏差，并区分本期需实现内容、暂不开发内容和待产品确认内容。

## 一、首页

### 1. 设备与套件

“设备与套件”页面需展示以下设备。设备卡片的数据摘要能力与展示状态如下：

| 设备名称 | 设备标识 | 数量 | 展示数据 | 本期状态 |
| --- | --- | ---: | --- | --- |
| 指尖夹爪 | `UMI_Fingers_L`、`UMI_Fingers_R` | 左、右各 1 个 | MKV 摘要、Y8 摘要、IMU 摘要、图标数据 | 需新增 |
| 板机夹爪 | `UMI_Grippers_L`、`UMI_Grippers_R` | 左、右各 1 个 | 无 | 暂未开发，卡片置灰 |
| 头部 Ego | `Ego_H` | 1 个 | MKV 摘要、Y8 摘要、IMU 摘要、图标数据 | 需新增 |
| 腕部 Ego | `Ego_W_L`、`Ego_W_R` | 左、右各 1 个 | MKV 摘要、Y8 摘要、IMU 摘要、图标数据 | 需新增 |
| 手套 | `Suits` | 1 套 | 无 | 暂未开发，卡片置灰 |

### 2. 设备商城

- 商城仅保留设备展示位，不提供下单能力。
- 展示内容为：`Ego_H` 1 个、`UMI_Grippers_L` 1 个、`UMI_Grippers_R` 1 个。
- 移除前端中的下单、价格和库存相关展示及接口接入。
- 每个展示位需提供在线、离线两种设备状态。

### 3. 精选内容

本期不实现精选内容的视频播放能力。播放流程、内容维护人及运营机制尚未确定，待产品侧明确后另行排期。

## 二、导航栏

- 底部导航固定展示“主页、数据、记录、我的”四个入口。
- 不支持通过左右滑动切换页面；仅通过导航项点击切换。

## 三、数据页

数据页用于展示设备连接、相机及采集任务状态，布局分为左侧设备状态区和右侧采集操作区。

### 1. 左侧：设备状态与详情

#### 设备状态卡片

| 设备 | 需展示的连接/工作状态 |
| --- | --- |
| `UMI_Fingers_L` | 无线连接：在线/离线；USB 连接：在线/离线 |
| `UMI_Fingers_R` | 无线连接：在线/离线；USB 连接：在线/离线 |
| `Ego_H` | USB 连接：在线/离线；双目：在线/离线；四目：在线/离线 |

- 每个设备卡片均可点击进入详情。
- 详情页至少展示设备名称及对应状态项的在线/离线状态。

#### `Ego_H` 详情

- 展示双目、四目状态，均支持在线/离线。
- 双目数据：MKV，分辨率 `4000 × 1200`。
- 双目数据：Y8，分辨率 `4000 × 1200`。
- 四目数据：Y8，分辨率 `3104 × 480`。

#### `UMI_Fingers_L` / `UMI_Fingers_R` 详情

- 展示无线连接、USB 连接状态，均支持在线/离线。
- 展示双目 MKV 数据，分辨率 `3840 × 1200`。
- `UMI_Fingers_R` 的详情能力与 `UMI_Fingers_L` 保持一致。

### 2. 右侧：相机与采集操作

- 删除数据页中的电量展示；设备电量统一由右上角电池区域展示，避免重复。
- 相机展示对象包括：头部双目、头部四目、左手双目、右手双目。

#### 待确认：Y8 数据展示

需明确右侧相机区域是否展示 Y8 数据流，以及展示范围（全部相机或仅部分相机）。

#### 待确认：任务领取与任务来源

任务信息需包含：设备类型、场景类型、任务范围、项目名称、子任务。

需确认任务获取方式：

- 从本地文件/介质导入；或
- 在 Web 端领取并同步至设备。

#### 待确认：录制界面与任务定义

需明确不同任务类型是否使用不同录制 UI，以及录制 UI 是否由任务配置动态定义。

#### 待确认：录制信息上报与数据存储

需确定录制信息上报后的数据存储位置：

- 外接存储设备；
- 云存储；
- 局域网独立服务端。

## 四、记录页

- 提供“采集记录”入口及记录列表展示。
- 记录字段、筛选条件、详情能力待后续产品需求补充。

## 五、待确认事项汇总

| 编号 | 事项 | 需产品确认的结论 |
| --- | --- | --- |
| 1 | 精选内容 | 内容来源、播放流程、维护责任人及后续排期 |
| 2 | 右侧相机 Y8 展示 | 是否展示，以及各相机的展示范围 |
| 3 | 任务领取 | 本地导入或 Web 端领取同步 |
| 4 | 录制 UI | 是否随任务类型变化、是否由任务配置定义 |
| 5 | 录制数据存储 | 外接存储、云存储或局域网服务端 |
| 6 | 采集记录 | 字段、筛选条件与详情展示规则 |
